- converter bug with mirrored

- all conv use bias, which we dont do 
- SE block use multiplicative and additive bias

- policy head is linear (ReLU + bn fehlt)
- policy head braucht kein fully connected. Direkt 73x8x8 oder 80x8x8 für output.
- output raw logits, softmax applied later (not that important)

- Value head 1x8x8 statt 32x8x8
- intermediate ist 128 via conv statt dense 256
- final Dense ist 128 zu 3 statt 256 zu 3
- (Conv -> Dense -> Dense) zu (Conv -> Conv -> Dense)
- move history ? (Off)
- value net bias towards draw (draw being 60%)
- Maybe Stockfish vs Komplett Naive
- SE Faktor 8 statt 16